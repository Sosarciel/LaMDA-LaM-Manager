# Zhipu (Z.ai) Chat Completion Response Schema

Sources:
- https://docs.z.ai/api-reference/llm/chat-completion (openapi yml: `zhipu-ai-paas-api-openapi.yml`)
- https://docs.z.ai/guides/capabilities/function-calling
- Full aggregated markdown: `llms-full.txt` (904KB)

## ChatCompletionResponse

```yaml
ChatCompletionResponse:
  type: object
  properties:
    id: string            # Task ID
    request_id: string    # Request ID
    created: integer      # Unix timestamp seconds
    model: string         # Model name
    choices:
      type: array
      items:
        type: object
        properties:
          index: integer
          message: { $ref: '#/components/schemas/ChatCompletionResponseMessage' }
          finish_reason:
            type: string
            description: 'Reason for model inference termination. Can be `stop`, `tool_calls`, `length`, `sensitive`, `model_context_window_exceeded` or `network_error`.'
    usage:
      type: object
      properties:
        prompt_tokens: number
        completion_tokens: number
        prompt_tokens_details: object
        # ... (see llms-full.txt for full usage details)
```

### ChatCompletionResponseMessage

```yaml
ChatCompletionResponseMessage:
  type: object
  properties:
    role: string          # default 'assistant'
    content: string       # "Hits function is null, otherwise returns model inference result"
    reasoning_content: string   # Reasoning content, supported by GLM-4.5 series
    tool_calls:
      type: array
      description: Function names and parameters generated by the model that should be called.
      items: { $ref: '#/components/schemas/ChatCompletionResponseMessageToolCall' }
```

> NOTE: `content` can be **null** when a function is hit.

### ChatCompletionResponseMessageToolCall

```yaml
ChatCompletionResponseMessageToolCall:
  type: object
  properties:
    function:
      type: object
      properties:
        name: string      # Model-generated function name
        arguments: object # JSON format of the function call parameters
      required: [name, arguments]
    id: string            # Unique identifier for the hit function
    type: string          # Tool type called by the model, currently only 'function'
```

> NOTE: In the zhipu openapi, tool call `arguments` is typed as `object`, but Deepseek/OpenAI type it as `string`. Validate in code.

## finish_reason enum (Zhipu)

`stop`, `tool_calls`, `length`, `sensitive`, `model_context_window_exceeded`, `network_error`.

(Note: differs from Deepseek's enum which uses `content_filter`, `insufficient_system_resource`.)

## GLM tool calling flow

- Request: `tools` array with `type:"function"` + `function` (name/description/parameters JSON Schema), plus optional `tool_choice`.
- Assistant message may return `tool_calls`.
- Client executes and appends a `tool` message: `{role:"tool", content, tool_call_id}`.
- `reasoning_content` present for GLM-4.5 series; thinking controlled by `thinking.type: enabled|disabled` and `thinking.clear_thinking` (default true).